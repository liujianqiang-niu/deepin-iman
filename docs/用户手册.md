# deepin-iman 用户手册

## 1. 简介

deepin-iman 是一款 AI 驱动的 man 手册查看器，专为 deepin/UOS 系统设计。它将传统的 man 手册浏览体验与 AI 能力深度融合，帮助用户更高效地学习和使用 Linux 命令。

## 2. 界面布局

应用主窗口分为以下几个区域：

```
┌─────────────────────────────────────────────────────────────────┐
│  标题栏：[← 后退] [→ 前进]  deepin man 手册  [菜单] [_] [□] [×]  │
├──────────┬────────────────────────────┬─────────────────────────┤
│          │                            │                         │
│ 左侧栏   │     man 手册阅读区          │     AI 助手面板          │
│          │     (HTML 渲染)             │                         │
│ 搜索框   │                            │  模型选择下拉框           │
│ 手册列表 │                            │  对话显示区              │
│          │                            │  输入框                  │
│          │                            │  [翻译][样例][提问][解析] │
│          │                            │                         │
├──────────┴────────────────────────────┴─────────────────────────┤
│                      嵌入终端（可隐藏）                           │
└─────────────────────────────────────────────────────────────────┘
```

- **左侧栏**：搜索框 + man 手册列表，用于查找和选择手册
- **阅读区**：以 HTML 格式渲染 man 手册内容，支持交叉引用链接跳转
- **AI 面板**：AI 助手对话区，提供翻译、样例生成、问答、命令解析等功能
- **终端**：嵌入式终端，可直接运行命令

## 3. 核心功能

### 3.1 man 手册浏览

1. 启动应用后，左侧栏会自动扫描并索引系统中所有 man 手册
2. 首次启动时会出现索引进度对话框，请耐心等待
3. 手册按名称排列在左侧栏列表中，点击即可打开

### 3.2 全文搜索

1. 在左侧栏顶部的搜索框中输入关键词
2. 系统使用 SQLite FTS5 全文索引进行搜索
3. 搜索结果会实时显示在下方列表中
4. 点击结果项即可打开对应手册

### 3.3 交叉引用导航

1. man 手册中的 SEE ALSO 部分包含交叉引用链接（如 `ls(1)`）
2. 点击链接可直接跳转到相关手册
3. 使用标题栏的 **后退**（←）和 **前进**（→）按钮或快捷键浏览历史
4. 快捷键：`Alt+Left` 后退，`Alt+Right` 前进

### 3.4 AI 翻译

1. 打开任意 man 手册后，AI 面板自动加载当前手册信息
2. 点击 **翻译** 按钮，AI 会将当前手册翻译为中文
3. 翻译结果会显示在 AI 面板的对话区

### 3.5 AI 使用样例生成

1. 打开任意 man 手册后，点击 **生成样例** 按钮
2. AI 会为当前命令生成 3-5 个实用使用样例
3. 每个样例包含：命令、注释说明、预期输出

### 3.6 AI 问答

1. 打开任意 man 手册后，在 AI 面板底部输入框中输入问题
2. 点击 **提问** 按钮
3. AI 会基于当前手册内容回答你的问题
4. 适用于理解复杂参数、查找特定用法等场景

### 3.7 命令解析

1. 在 AI 面板输入框中输入一条命令（如 `sudo apt install -y vim`）
2. 点击 **解析命令** 按钮
3. 系统会自动提取命令名并跳转到对应的 man 手册

### 3.8 嵌入终端

1. 点击标题栏菜单中的 **终端** 选项
2. 终端面板会在窗口底部展开/收起
3. 可以直接在终端中运行命令，验证手册中描述的用法

### 3.9 收藏

1. 打开手册后，点击标题栏菜单中的 **收藏当前页**
2. 再次点击可取消收藏
3. 便于快速访问常用手册

### 3.10 历史记录

系统会自动记录你浏览过的手册，方便回顾学习历史。

## 4. AI 模型设置

### 4.1 打开设置

点击标题栏右上角菜单 → **AI 设置**，打开 AI 模型厂商管理界面。

### 4.2 内置模型厂商

deepin-iman 预置以下 OpenAI 兼容的模型厂商：

| 厂商 | Base URL | 默认模型 |
|------|---------|---------|
| OpenAI | `https://api.openai.com/v1` | gpt-4o |
| 智谱 GLM | `https://open.bigmodel.cn/api/paas/v4` | glm-4-plus |
| 通义千问 | `https://dashscope.aliyuncs.com/compatible-mode/v1` | qwen-max |
| DeepSeek | `https://api.deepseek.com/v1` | deepseek-chat |

> **说明**：Base URL 是 API 的基础地址，不需要包含 `/chat/completions` 路径，应用会自动拼接。

### 4.3 配置模型厂商

1. 在厂商列表中选中要配置的厂商
2. 在编辑区域填写以下信息：
   - **厂商名称**：显示名称，可自定义
   - **Base URL**：厂商的 OpenAI 兼容 API 基础地址（如 `https://api.openai.com/v1`）
   - **API Key**：在厂商平台申请的 API 密钥
   - **模型名称**：要使用的模型名称
3. 点击 **测试连接** 验证配置是否正确，状态栏会显示连接结果
4. 点击 **设为当前** 将该厂商设为默认使用的模型
5. 点击 **保存** 按钮保存所有配置

### 4.4 添加自定义厂商

deepin-iman 兼容所有 OpenAI API 格式的厂商。

1. 点击 **添加厂商** 按钮
2. 在编辑区域填写厂商信息：
   - **厂商名称**：如 "OpenRouter" 或 "本地模型"
   - **Base URL**：OpenAI 兼容的 API 基础地址（通常以 `/v1` 结尾）
   - **API Key**：对应厂商的 API 密钥
   - **模型名称**：如 `gpt-4o-mini`、`deepseek-reasoner` 等
3. 点击 **测试连接** 验证配置
4. 点击 **设为当前** 选择该厂商
5. 点击 **保存**

常见 OpenAI 兼容厂商示例：

| 厂商 | Base URL | 模型示例 |
|------|---------|---------|
| OpenRouter | `https://openrouter.ai/api/v1` | `anthropic/claude-3.5-sonnet` |
| Ollama 本地 | `http://localhost:11434/v1` | `llama3.1` |
| 硅基流动 | `https://api.siliconflow.cn/v1` | `Qwen/Qwen2.5-72B-Instruct` |
| Moonshot | `https://api.moonshot.cn/v1` | `moonshot-v1-8k` |

### 4.5 测试连接

在配置或编辑厂商信息时，点击 **测试连接** 按钮，应用会向该厂商发送一条测试消息：

- **连接成功**：状态栏显示绿色 ✓ 标记和 AI 回复内容
- **连接失败**：状态栏显示红色 ✗ 标记和错误信息

建议在保存配置前先测试连接，确保 Base URL、API Key 和模型名称正确。

### 4.6 删除模型厂商

1. 在列表中选中要删除的厂商
2. 点击 **删除厂商** 按钮
3. 如果删除的是当前使用的厂商，系统会自动切换到列表中的第一个厂商

### 4.7 主界面切换模型

在 AI 面板顶部的下拉框中，可以随时切换当前使用的模型厂商，无需每次打开设置。切换后，下方的"当前模型"标签会实时更新显示厂商名称和模型名称。

### 4.8 查看当前模型

AI 面板标题下方显示"当前模型：厂商名 / 模型名"，让你随时知道正在使用哪个模型。每条 AI 回复消息末尾也会标注 `[模型名]`，方便追溯。

## 5. 菜单功能

标题栏右上角菜单包含以下选项：

| 菜单项 | 功能 |
|--------|------|
| AI 设置 | 打开 AI 模型厂商管理界面 |
| 终端 | 显示/隐藏嵌入式终端 |
| 收藏当前页 | 收藏或取消收藏当前手册 |
| 关于 | 查看应用信息（由系统统一提供） |

## 6. 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Alt+Left` | 后退到上一页 |
| `Alt+Right` | 前进到下一页 |

## 7. 数据存储

应用数据存储在以下位置：

| 数据类型 | 路径 |
|---------|------|
| 手册索引 | `~/.local/share/deepin/deepin-iman/manindex.db` |
| 翻译缓存 | `~/.local/share/deepin/deepin-iman/translation.db` |
| 收藏记录 | `~/.local/share/deepin/deepin-iman/favorite.db` |
| 浏览历史 | `~/.local/share/deepin/deepin-iman/history.db` |
| 配置文件 | `~/.config/deepin/deepin-iman.conf` |

## 8. 依赖

运行 deepin-iman 需要以下组件：

- **mandoc**：man 手册 HTML 渲染引擎（必须）
- **manpages-zh** 或 **manpages-cn**：中文 man 手册包（推荐）
- **libqtermwidget6-2**：嵌入终端组件（推荐）

安装命令：

```bash
sudo apt install mandoc manpages-zh
```

## 9. 许可证

本项目基于 **LGPL-3.0+** 许可证发布。

Copyright (C) 2026 UnionTech Software Technology Co., Ltd.
